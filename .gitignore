# Jekyll Build-Ausgabe
_site/
.jekyll-cache/
.jekyll-metadata

# Ruby / Bundler (nur für optionale lokale Vorschau)
vendor/
Gemfile.lock
.bundle/

# Betriebssystem
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp
*.swo
*~

# Claude Code — nur lokale Overrides ignorieren, geteilte Einstellungen committen
.claude/settings.local.json

# Temporäre Dateien
*.tmp
*.bak
.cache/

# Secrets (defensiv — aktuell keine env-Dateien im Einsatz)
.env
.env.*
